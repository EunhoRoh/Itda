services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: itda
      POSTGRES_USER: itda
      POSTGRES_PASSWORD: itda-local
    ports:
      - "5433:5432"
    volumes:
      - itda-db:/var/lib/postgresql/data

volumes:
  itda-db:
